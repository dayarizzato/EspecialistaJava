package arrays.ornedacaoArrays;

import java.util.Arrays;

public class OrdemCrescente {
    public static void main(String[] args) {
        int[] numeros = {20, 4, 29, 2};

        Arrays.sort(numeros);

        System.out.println(Arrays.toString(numeros));
    }
}
